# echo
输出的内容，类似于cout<<（加不加双引号都行）

# 反引号
eg. echo `pwd` 输出pwd的结果

# 重定向符
- `>`：将左侧命令的结果，覆盖写入到符号右侧地点
- `>>`：将左侧命令的结果，追加写入到符号右侧地点

# Tail
## 基本语法
Tail [-f -num] Linux路径

查看文件尾部内容

- `-f`：表示持续跟踪（增加的会新展示，不会覆盖之前的）
- `-num`：表示查看尾部多少行，不填默认10行
