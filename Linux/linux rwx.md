# ls -lh 权限说明

ls -lh 后列表前十位：权限控制信息（1+3+3）

## 第一位：文件类型
- `-`：表示文件
- `d`：表示文件夹（directory）
- `l`：表示软链接

## 权限位说明（rwx）
- `r`：read，可读
- `w`：write，可写
- `x`：execute，可执行

有该权限时显示字母，没有时显示 `-`

## 三组权限
rwx 有三组，分别代表：

1. **第一组**：所属用户的权限
2. **第二组**：所属用户组的权限
3. **第三组**：其他用户的权限
